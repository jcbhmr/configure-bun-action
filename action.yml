name: Configure Bun action
description: "🧅 Write your GitHub Actions using Bun!"
branding:
  icon: settings
  color: yellow
inputs:
  token:
    default: ${{ github.token }}
  path:
    default: .
runs:
  using: node20
  main: .bun/main.mjs
